{
  "name": "whatsapp-bot",
  "description": "WhatsApp bot scaffold (Baileys)",
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ],
  "env": {
    "PORT": {
      "description": "Port to bind the web server (if any)"
    },
    "SESSION_FILE": {
      "description": "Path to auth file used by Baileys",
      "value": "auth_info.json"
    }
  }
}